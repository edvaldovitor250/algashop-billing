server:
  port: 8082

spring:
  application:
    name: billing
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration

algashop:
  integrations:
    payment:
      provider: FASTPAY #FAKE
